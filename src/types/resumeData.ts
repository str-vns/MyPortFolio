export interface Educations {
  school: string;
  degree: string;
  duration: string;
  course: string;
  icon: React.ReactNode;
  iconColor?: string;
}

export interface Experience {
    company: string;
    position: string;
    duration: string;
    description: string;
    icon: React.ReactNode;
    iconColor?: string;
    }